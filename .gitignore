# ---------------------------------------------------------------------------
# Bulk data (~2.2 GB) — NOT in git. Individual files exceed GitHub's 100 MB
# hard limit. Everything under data/ is public source data, re-downloadable
# with `bash scripts/fetch_boston_open_data.sh` and rebuildable per-AOI with
# `python scripts/build_aoi.py --aoi <name>`. See DATA_MANIFEST.md.
# ---------------------------------------------------------------------------
data/

# Simulation outputs and caches — regenerated by the pipeline
runs/

# Course/proposal documents — kept local, not published
Project Proposal.docx
*.docx

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/

# Editors / OS
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
node_modules/
dist/
.vite/
gui/public/data/

# Office lock files
~$*
